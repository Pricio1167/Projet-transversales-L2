export const colors = {
  primary: "#334155",
  blanc: "#ffffff",
  surface: "#f3f4f6",
  grisClair: "#f3f4f6",
  vert: "#16a34a",
  orange: "#d97706",
  danger: "#b91c1c",
  rougeTrafic: "#ef4444",
  violet: "#6b5b95",
  texte: "#0f172a",
  texteMuted: "#475569",
  bordure: "#d1d5db",
};

// Backwards-compat aliases (some components use old keys)
colors.bleu = colors.primary;
colors.blanc = colors.blanc;
colors.grisClair = colors.grisClair;
colors.vert = colors.vert;
colors.orange = colors.orange;
colors.rouge = colors.danger;
colors.texte = colors.texte;
colors.texteMuted = colors.texteMuted;
colors.bordure = colors.bordure;

export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
};
